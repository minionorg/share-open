/**
 * 判断是为空的对象
 *
 * @param {object} obj 将要判断的对象
 * @return {boolean} 空为真
 * @example
 * isEmpty({a: 1}) = false
 * isEmpty({}) = true
 */
declare function isEmpty(obj: object): boolean;
export default isEmpty;
